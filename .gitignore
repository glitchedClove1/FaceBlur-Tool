.venv/
HANDOFF.md
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# Data (downloaded separately, see scripts/download_widerface.py)
data/widerface/*
!data/widerface/.gitkeep

# Training artifacts
runs/
checkpoints/*
*.pt
outputs/
# Exception: best.pth is committed deliberately (~47MB, well under GitHub's
# limits) so deployment platforms (Render, HF Spaces) can pull weights
# straight from the repo without a separate upload/download step.
!checkpoints/best.pth
logs/
.tensorboard/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
*.egg-info/
