# Trimmed dependency set for the Gradio deployment (Hugging Face Spaces free
# CPU tier) - this is NOT the training environment's requirements.txt.
# Excludes everything training-only: albumentations, tensorboard, pytest,
# gdown, scipy (only used by engine/evaluate.py, not the deployed app).
#
# CPU-only torch/torchvision (no --extra-index-url override needed - the
# plain PyPI package names ship CPU wheels by default). A free Space has no
# GPU, and installing CUDA wheels there just wastes build time and disk
# for a backend that will never be used.
torch==2.13.0
torchvision==0.28.0

numpy==2.5.2
opencv-python-headless==5.0.0.93
pyyaml==6.0.3
gradio==6.26.0
